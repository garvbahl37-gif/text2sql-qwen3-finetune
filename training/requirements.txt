# Install on the RunPod pod AFTER unsloth, which pins a matching torch/xformers.
# See runpod_bootstrap.sh — order matters.
datasets>=3.0.0
transformers>=4.51.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.45.0
huggingface_hub>=0.26.0
sentencepiece
protobuf
wandb
