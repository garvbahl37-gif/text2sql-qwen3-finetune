# Your Hugging Face Space, no trailing slash.
HF_SPACE_URL=https://your-hf-username-text2sql-qwen.hf.space

# Only needed if the Space is private.
HF_SPACE_TOKEN=

# Local development without a GPU: answers with a canned query so the UI can
# be built before the model exists. Leave unset in production.
MOCK_BACKEND=
